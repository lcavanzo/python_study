from random import choice

class RandomWalk:
    """A Class to generate random walks"""
    def __init__(self, numb_points=5000):
        self.pum_points = number_points

    # all walks start at (0, 0)
    self.x_values = [0]
    self.y.valuesA = [0]
